spring:
  application:
    name: AREcommerceApi
  data:
    mongodb:
      uri: mongodb://localhost:27017/arecommerce-test
  mongodb:
    embedded:
      version: 6.0.5

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 6.0.5

yandex-s3:
  endpoint: https://storage.yandexcloud.net
  region: ru-central1
  access-key-id: test-access-key
  secret-access-key: test-secret-key
  buckets:
    models: ar-models-test
    images: product-images-test

admin:
  api-key: test-admin-key
